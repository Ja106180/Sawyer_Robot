# 第3课：一维卡尔曼滤波 - 从最简单开始

## 🎓 学习目标
实现一个最简单的一维卡尔曼滤波器，理解预测-更新循环

## 📖 场景设定

**问题**：估计房间的温度

- **真实温度**：我们不知道（这是我们要估计的）
- **温度计测量**：有噪声，不准确
- **模型**：温度变化很慢，基本不变

## 🔄 卡尔曼滤波步骤

### 步骤1：初始化
```cpp
// 初始估计
float temperature = 20.0;  // 猜测初始温度
float uncertainty = 10.0;  // 初始不确定性（很大，因为我们不确定）
```

### 步骤2：预测（Prediction）
```cpp
// 根据模型预测（温度基本不变）
temperature_predicted = temperature;  // 温度不变
uncertainty_predicted = uncertainty + process_noise;  // 不确定性增加
```

### 步骤3：更新（Update）
```cpp
// 收到测量值
float measurement = 21.5;  // 温度计读数
float measurement_uncertainty = 2.0;  // 温度计的误差

// 计算卡尔曼增益
float kalman_gain = uncertainty_predicted / 
                    (uncertainty_predicted + measurement_uncertainty);

// 融合预测和测量
temperature = temperature_predicted + 
              kalman_gain * (measurement - temperature_predicted);

// 更新不确定性（变小了，因为有了新信息）
uncertainty = (1 - kalman_gain) * uncertainty_predicted;
```

### 步骤4：重复
回到步骤2，继续预测-更新循环

## 💡 关键理解

### 卡尔曼增益（Kalman Gain）
- **范围**：0 到 1
- **含义**：
  - 接近1：更相信测量（预测不确定性大）
  - 接近0：更相信预测（测量不确定性大）

### 不确定性变化
- **预测后**：不确定性增加（因为我们不完美）
- **更新后**：不确定性减少（因为有了新信息）

## 📁 文件说明

- `simple_kalman.cpp`：完整的一维卡尔曼滤波实现
- `CMakeLists.txt`：编译配置
- `run_example.sh`：运行脚本

## 🚀 下一步
运行代码，观察滤波效果！

